/**
 * Public browser-side analytics identifiers that must stay aligned with their
 * server-side counterparts. These are routing IDs, not secrets.
 */
export const META_BROWSER_PIXEL_ID = "1668160384441545";
